import setuptools
setuptools.setup(name='rawtypes',
                 version='1.0',
                 package_dir={'': 'src'},
                 packages=setuptools.find_packages("src"),
                 )
